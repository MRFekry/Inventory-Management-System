@inject NavigationManager NavigationManager
@inject IDeleteProductUseCase DeleteProductUseCase

@if (Product != null)
{
    <tr>
        <td>@Product.Name</td>
        <td>@Product.Quantity</td>
        <td>@Product.Price.ToString("C")</td>
        <td>
            <a class="btn btn-primary" href="@($"/products/edit/{Product.Id}")">
                Edit
            </a>
        </td>
        <td>
            <EditForm Enhance="true" FormName="@($"DeleteProductForm-{Product.Id}")" Model="Product" OnValidSubmit="() => DeleteProduct(Product.Id)">
                <button class="btn btn-danger" type="submit">
                    Delete
                </button>
            </EditForm>
        </td>
    </tr>
}

@code {
    [Parameter]
    public Product? Product { get; set; }

    private void DeleteProduct(int productId)
    {
        DeleteProductUseCase.ExecuteAsync(productId);
        NavigationManager.Refresh();
    }
}
